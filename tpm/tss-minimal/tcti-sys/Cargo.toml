[package]
name = "tcti-sys"
version = "0.1.0"
edition = "2021"
links = "tss2-tctildr"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[build-dependencies]
bindgen = { version = "0.59", features = ["runtime"], default-features = false }
pkg-config = "0.3"
