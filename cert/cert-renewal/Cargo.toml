[package]
name = "cert-renewal"
version = "0.1.0"
authors = ["Azure IoT Edge Devs"]
edition = "2021"

[dependencies]
openssl = "0.10"
serde = "1"

[dev-dependencies]
serial_test = "0.5"
toml = "0.5"

test-common = { path = "../../test-common" }
